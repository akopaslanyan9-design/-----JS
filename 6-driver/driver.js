const hasLicence = true;
const age = Number(prompt("Введите ваш возраст"));
const isDrunk = false;

if (age >= 18  
   && hasLicence 
   && isDrunk) {
    console.log('может');
   }else {
    console.log('не может');
   }
